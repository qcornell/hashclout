# Server Configuration
PORT=3000
NODE_ENV=development

# MongoDB Connection (Update with your actual MongoDB connection string)
MONGODB_URI=mongodb://localhost:27017/hashclout

# JWT Secret (Generate a strong random string for production)
JWT_SECRET=your-secret-jwt-token-key-replace-in-production

# Email Configuration (if using email features)
EMAIL_SERVICE=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-email-app-password

# Hedera API keys/secrets (if connecting to Hedera)
HEDERA_ACCOUNT_ID=your-hedera-account-id
HEDERA_PRIVATE_KEY=your-hedera-private-key